package net.fununity.games.skydilation.chunkgeneration.distribution;

import org.bukkit.Material;

public class UpperBlockPopulator extends SkyDilationDistribution {
    public UpperBlockPopulator(Material material, byte data, int tries, int percentage) {
        super(material, data, tries, percentage);
    }
}
