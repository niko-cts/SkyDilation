package net.fununity.games.skydilation.chunkgeneration.distribution;

import org.bukkit.Material;

public class OreDistribution extends SkyDilationDistribution {
    public OreDistribution(Material material, byte data, int tries, float percentage) {
        super(material, data, tries, percentage);
    }
}
